# Frontend (Vite + React + Tailwind)

Install & run:
```bash
cd frontend
npm install
# copy .env.example to .env and edit VITE_API_URL if needed
npm run dev
```

This is a minimal scaffold with sidebar and placeholder pages. Replace placeholders with full components.
